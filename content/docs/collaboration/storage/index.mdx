---
title: Storage
description: Where document states, versions and comments persist.
icon: Database
---

# Storage

The `CollaborationStorage` interface is the persistence seam. `lucidStorage({ connection })` resolves the Lucid connection from inside the library — no `import db` in your config.

## Deep dives

- [Lucid storage](/docs/collaboration/storage/lucid)
